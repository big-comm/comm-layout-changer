# Maintainer: Tales A. Mendonça <talesam@gmail.com>
<<<<<<< Updated upstream

pkgname=gnome-themes
pkgdesc="app to apply pre-customized desktop layouts"
# Uncomment and edit the following lines as needed
#depends=('')
#makedepends=('')
#optdepends=('')
=======
# Contributor: Ari Novais (Float) <floatingfloatskies@protonmail.com>

pkgname=comm-layout-switcher
pkgdesc="App to apply pre-customized desktop layouts for GNOME"
# Uncomment and edit the following lines as needed
depends=(
    'gtk4'
    'libadwaita'
    'python'
    'python-gobject'
    'python-reportlab'
    'python-pyenchant'
)
makedepends=('')
optdepends=(
    
)
>>>>>>> Stashed changes
#conflicts=('')
#provides=('')
#replaces=('')
pkgver=$(date +%y.%m.%d)
pkgrel=$(date +%H%M)
arch=('any')
license=('MIT')
<<<<<<< Updated upstream
url="https://github.com/big-comm/${pkgname}"
=======
<<<<<<< Updated upstream
url="https://github.com/big-comm/comm-layout-changer"
depends=('python' 'python-gobject' 'python-cairo' 'libadwaita' 'dconf' 'hicolor-icon-theme')
makedepends=('git' 'python-setuptools')
optdepends=('gnome-shell: for GNOME desktop support'
            'cinnamon: for Cinnamon desktop support'
            'xfce4-panel: for XFCE desktop support')
conflicts=()
provides=()
replaces=()
=======
url="https://github.com/big-comm/${pkgname}"
>>>>>>> Stashed changes
>>>>>>> Stashed changes
source=("git+${url}.git")
md5sums=('SKIP')

# Automatically detect and use the correct install file
if [ -e "${pkgname}.install" ]; then
    install=${pkgname}.install
elif [ -e "pkgbuild.install" ]; then
    install=pkgbuild.install
fi

prepare() {
    cd "${srcdir}/${pkgname}"
    # Add any preparation steps here, if needed
    # For example: patch -p1 < "${srcdir}/patch-file.patch"
}

build() {
    cd "${srcdir}/${pkgname}"
    # Add build steps here, if needed
    # For example: make
}

check() {
    cd "${srcdir}/${pkgname}"
    # Add check/test steps here, if needed
    # For example: make check
}

package() {
    cd "${srcdir}/${pkgname}"

    # Determine the correct source directory
    if [ -d "${pkgname}" ]; then
        srcdir="${srcdir}/${pkgname}/${pkgname}"
    else
        srcdir="${srcdir}/${pkgname}"
    fi

    # Install files
    local dirs=("usr" "etc" "opt")
    for dir in "${dirs[@]}"; do
        if [ -d "${srcdir}/${dir}" ]; then
            cp -a "${srcdir}/${dir}" "${pkgdir}/"
        fi
    done

    # Install license file if present
    if [ -f "LICENSE" ]; then
        install -Dm644 LICENSE "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
    fi

    # Install documentation if present
    if [ -f "README.md" ]; then
        install -Dm644 README.md "${pkgdir}/usr/share/doc/${pkgname}/README.md"
    fi
}
